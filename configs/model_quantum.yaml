hybrid_quantum:
  n_qubits: 4
  n_layers: 3
  n_classes: 4
  encoding: "angle"  # Options: amplitude, angle, hybrid
  latent_dim: 16
  entanglement: "full"
  measurement: "probs"
  lr: 0.01
  batch_size: 16
  n_epochs: 50
  optimizer: "adam"
  patience: 20
  device: "default.qubit"

ablation:
  n_qubits: [2, 4, 6, 8]
  n_layers: [1, 2, 3, 4, 5]
  encodings: ["amplitude", "angle"]
  latent_dims: [8, 16, 32]
